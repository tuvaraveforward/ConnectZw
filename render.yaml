services:
  - type: web
    name: Project1
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt && python manage.py collectstatic --noinput && python manage.py migrate
    startCommand: gunicorn Project1.wsgi:application --bind 0.0.0.0:$PORT
